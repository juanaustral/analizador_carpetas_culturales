name: Keep Render Alive

on:
  schedule:
    # Cada 10 minutos, las 24 horas del día
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        run: |
          curl -s -o /dev/null -w "%{http_code}" \
            "https://analizador-carpetas-culturales.onrender.com/" \
            --max-time 30
